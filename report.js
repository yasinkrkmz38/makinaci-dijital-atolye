'use strict';
const root=document.getElementById('reportRoot'),escapeHtml=value=>String(value??'').replace(/[&<>"']/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[char])),date=value=>value?new Date(value).toLocaleString('tr-TR',{dateStyle:'short',timeStyle:'short'}):'—',money=value=>new Intl.NumberFormat('tr-TR',{style:'currency',currency:'TRY'}).format(Number(value)||0);
function cell(label,value){return `<div class="cell"><small>${escapeHtml(label)}</small><strong>${escapeHtml(value??'—')}</strong></div>`}
async function load(){const parts=location.pathname.split('/').filter(Boolean),type=parts[1]==='maintenance'?'maintenance':'work-order',id=parts[2],response=await fetch(`/api/reports/${type}/${encodeURIComponent(id)}`),data=await response.json();if(!response.ok)throw Error(data.error||'Rapor yüklenemedi');const record=data.record,isMaintenance=type==='maintenance',title=isMaintenance?'BAKIM / SERVİS RAPORU':'İŞ EMRİ / SERVİS RAPORU',task=isMaintenance?record.task:record.title,status=record.status==='done'?'Tamamlandı':record.status==='in_progress'?'İşlemde':record.status==='waiting'?'Bekliyor':'Açık',technician=isMaintenance?record.technician_member_name:record.assigned_member_name;document.title=`${data.report_no} • ${task}`;root.innerHTML=`<div class="head"><div class="brand">${data.company_logo_attachment_id?`<img src="/api/attachments/${data.company_logo_attachment_id}" alt="Firma logosu">`:'⚙'}<span>DİJİTAL MAKİNACI</span></div><div class="meta"><b>${escapeHtml(record.company_name)}</b>${escapeHtml(data.report_no)}<br>${date(data.generated_at)}</div></div><h1 class="title">${title}</h1><div class="machine"><b>${escapeHtml(record.machine_name||'Makine belirtilmedi')}</b><br>${escapeHtml([record.manufacturer,record.model,record.serial_no,record.location].filter(Boolean).join(' • ')||'Teknik kimlik bilgisi yok')}</div><div class="grid">${cell('İş / görev',task)}${cell('Durum',status)}${cell('Sorumlu',technician)}${cell('Plan / termin',record.due_date)}${cell('Gerçek süre',`${record.actual_duration_min||record.duration_min||0} dk`)}${cell('Maliyet',money(record.cost))}</div><div class="section"><h3>Yapılan iş / açıklama</h3><p>${escapeHtml(isMaintenance?record.note:record.description||'—')}</p></div><div class="section"><h3>Checklist ve ölçümler</h3><div class="rows">${data.checklist.map(item=>`<div><span>${escapeHtml(item.label)} • ${escapeHtml(item.result_status)}</span><b>${escapeHtml([item.measurement_value,item.measurement_unit,item.note].filter(Boolean).join(' • ')||'—')}</b></div>`).join('')||'—'}</div></div><div class="section"><h3>Kullanılan parçalar</h3><div class="rows">${data.parts.map(item=>`<div><span>${escapeHtml(item.part_name)} (${escapeHtml(item.part_code||'kod yok')})</span><b>${escapeHtml(item.quantity)} ${escapeHtml(item.unit)}</b></div>`).join('')||'—'}</div></div>${data.measurements.length?`<div class="section"><h3>Son ölçümler</h3><div class="rows">${data.measurements.map(item=>`<div><span>${date(item.measured_at)}</span><b>${escapeHtml(['Sıcaklık '+(item.temperature??'—'),'Basınç '+(item.pressure??'—'),'Titreşim '+(item.vibration??'—')].join(' • '))}</b></div>`).join('')}</div></div>`:''}${data.attachments.some(file=>file.mime_type.startsWith('image/'))?`<div class="section"><h3>Fotoğraflar</h3><div class="evidence">${data.attachments.filter(file=>file.mime_type.startsWith('image/')).slice(0,6).map(file=>`<img src="/api/attachments/${file.id}" alt="${escapeHtml(file.caption||file.file_name)}">`).join('')}</div></div>`:''}<div class="signatures">${['technician','customer'].map(kind=>{const signature=data.signatures.find(item=>item.signer_type===kind);return `<div class="signature">${signature?`<img src="${signature.signature_data}" alt="İmza"><b>${escapeHtml(signature.signer_name)}</b><br>`:''}${kind==='technician'?'Uygulayan teknisyen':'Müşteri / onay'} imzası</div>`}).join('')}</div><div class="foot"><span>Dijital Makinacı V17.3.3 • Oluşturan: ${escapeHtml(data.generated_by)}</span><img class="qr" src="${data.qr_data_url}" alt="Rapor QR"></div>`}
document.getElementById('printReport').addEventListener('click',()=>window.print());load().catch(error=>root.innerHTML=`<h1>Rapor açılamadı</h1><p>${escapeHtml(error.message)}</p>`);
