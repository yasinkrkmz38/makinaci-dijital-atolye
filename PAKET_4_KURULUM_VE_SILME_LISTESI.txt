DİJİTAL MAKİNACI V17.2 — PAKET 4 / REPO TEMİZLİĞİ

A) ÖNCE ZIP İÇİNDEKİ DOSYALARI YÜKLE

Repo kökü:
  .gitignore
  .env.example

public klasörü:
  public/index.html  -> mevcut dosyanın üzerine yaz

NOT:
- package.json değişmiyor.
- server.js değişmiyor.
- v1621-index.html değişmiyor.
- v1621-app.js değişmiyor.
- v172-integrity.js değişmiyor.
- v1621-service-worker.js değişmiyor.
- PostgreSQL verilerine dokunulmuyor.
Bu nedenle çalışan V17 uygulamayı bozmaması amaçlanmıştır.

B) SONRA GITHUB'DAN BU 3 ESKİ SQLITE DOSYASINI SİL

  atolye.db
  atolye.db-shm
  atolye.db-wal

Mevcut server.js PostgreSQL (pg / DATABASE_URL) kullanıyor.
Bu üç dosya canlı PostgreSQL veritabanı değildir.

C) GITHUB KÖKÜNDEKİ AŞAĞIDAKİ ESKİ DEPENDENCY KALINTILARI DA SİLİNEBİLİR

  simple-concat/
  simple-get/
  statuses/
  string_decoder/
  strip-json-comments/
  tar-fs/
  tar-stream/
  toidentifier/
  tunnel-agent/
  type-is/
  unpipe/

Bunlar node_modules klasörü yerine yanlışlıkla repo köküne commit edilmiş paket kalıntılarıdır.
Normal bağımlılıklar Render deploy sırasında package.json üzerinden kurulur.

D) ŞİMDİLİK SİLME

Aşağıdakilere henüz dokunma:
  server.js
  package.json
  package-lock.json
  v1621-index.html
  v1621-app.js
  v1621-style.css
  v1621-service-worker.js
  v1621-manifest.webmanifest
  appstore-v17.css
  v172-integrity.js
  public/
  admin dosyaları

Eski README / V6-V16 kurulum belgelerini de bu pakette silmiyoruz.
Frontend konsolidasyonundan sonra tek seferde ayıklayacağız.

E) TEST

Commit sonrası Render deploy başarılı olmalı.

Kontrol:
1. Ana site açılıyor mu?
2. Mobil V17 tasarım duruyor mu?
3. /rastgele-test gibi bilinmeyen bir URL açıldığında eski V16 ekranı yerine / ana sayfasına dönüyor mu?
4. Stok kartı düzenlerken miktar alanı kilitli mi?
5. Delme hesabında çap 0 girilince hata veriyor mu?

Paket: V17.2 / Paket 4
